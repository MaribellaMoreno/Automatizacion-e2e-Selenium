{
  "name": "selenium",
  "version": "1.0.0",
  "description": "Proyecto final Test Automation Engineer",
  "scripts": {
    "test": "npx wdio run wdio.conf.js", 
    "wdio": "npx wdio run wdio.conf.js",
    "generate:metadata": "node generate-allure-metadata.js",
    "allure:generate": "allure generate reports/allure-results --clean",
    "allure:open": "allure open",
    "allure:report": "npm run generate:metadata && npx allure generate reports/allure-results --clean && npx allure open"
  },
  "keywords": [],
  "author": "Maribella Moreno",
  "license": "ISC",
  "dependencies": {
    "@wdio/cli": "^9.18.2"
  },
  "devDependencies": {
    "@wdio/allure-reporter": "^9.18.0",
    "@wdio/cucumber-framework": "^9.18.0",
    "@wdio/local-runner": "^9.18.1",
    "@wdio/spec-reporter": "^9.18.0"
  }
}